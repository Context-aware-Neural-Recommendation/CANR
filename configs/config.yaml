project:
  name: context-aware-neural-recommendation-engine
  version: 0.1.0

data:
  raw_path: data/raw
  processed_path: data/processed
  sample_path: data/sample

model:
  embedding_dimension: 64
  top_k: 10

redis:
  host: localhost
  port: 6379
  db: 0

api:
  host: 0.0.0.0
  port: 8000